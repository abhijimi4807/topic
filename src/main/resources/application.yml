server:
  port: 9001
spring :
  application :
    name : topics-service
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    hostname : localhost
    serviceUrl :
      defaultZone : http://localhost:8761/eureka/